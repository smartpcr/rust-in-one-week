# cmd

Demonstrates:

- Actix-web server setup with routing (`/` and `/gcd` endpoints)
- Module visibility (`mod my` with nested modules)
- Form handling with `serde` deserialization
- HTML response generation

The server runs on `http://localhost:3000` and provides a GCD calculator web interface.

## Run

```bash
cargo run -p cmd
```
